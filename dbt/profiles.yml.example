devtrend:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      dbname: devtrend_db
      user: devtrend_user
      password: "{{ env_var('DB_PASSWORD') }}"
      schema: public
      threads: 4